.pi/

## Rust build output
**/target/
target

## Compiled tool binaries (cargo builds to target/debug)
tools/*/bin/

## Runtime data (created by the harness)
sessions/

## Scratch test file
test.txt

## Python bytecode
__pycache__/

## Embedded reference repos (external checkouts; never part of the
## distribution, so they must never be swept in by `git add -A`)
scratch/refs/pi-vim
