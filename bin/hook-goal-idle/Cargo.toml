[package]
name = "hook-goal-idle"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "harness-hook-goal-idle"
path = "src/main.rs"

[dependencies]
goal-state = { path = "../../crates/goal-state" }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
